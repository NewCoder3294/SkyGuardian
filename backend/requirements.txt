# Backend (laptop brain) runtime deps.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
websockets>=12.0
python-multipart>=0.0.9   # FastAPI file uploads (operator video source upload)

# Perception — GPS-less monocular mapping (Track 2, built)
numpy>=1.26
opencv-python-headless>=4.9   # VO geometry + frame I/O (use opencv-python if you need GUI windows)
pupil-apriltags>=1.0          # metric scale anchor + follow controller (Track 1)

# Perception — YOLO detection (local weights, no cloud)
ultralytics>=8.1

# Track 1 robotics — Tello control + follow controller
djitellopy>=2.5

# On-device reasoning (Ollama client) + Foundry export — runtime HTTP client
httpx>=0.27
