// Re-export of the shared wire contracts so app code can import from `@/lib/contracts`.
// Source of truth lives in /shared/contracts.ts (TS mirror of backend/app/contracts.py).
export * from "../../../shared/contracts";
