# `scripts/` — Operational helpers

Small standalone scripts for bring-up, networking, and dev convenience.

## Planned
- `send_intent.py` — scripted WS intent sender to drive the state machine without the UI.
- `check_routing.sh` — `route get` per interface to verify the dual-WiFi networking
  (Tello AP vs. phone network). See CLAUDE.md "Networking".
- `record_mavic.py` — capture a Mavic clip into `captures/mavic/` for perception dev.
